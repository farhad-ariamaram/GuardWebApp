@page
@model GuardWebApp.Pages.GuardAreaAllocationPage.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h3>از حذف مورد زیر مطمئن هستید؟</h3>
<br />
<div>
    <h4>تخصیص محل گشت</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            تاریخ شروع
        </dt>
        <dd class="col-sm-10">
            @try
            {
                @Model.GuardAreaAllocation.StartDate.removeYear();
            }
            catch (Exception)
            {
            }
        </dd>
        <dt class="col-sm-2">
            تاریخ پایان
        </dt>
        <dd class="col-sm-10">
            @try
            {
                @Model.GuardAreaAllocation.EndDate.Value.removeYear();
            }
            catch (Exception)
            {
            }
        </dd>
        <dt class="col-sm-2">
            محل گشت
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.GuardAreaAllocation.GuardArea.Description)
        </dd>
        <dt class="col-sm-2">
            نگهبان
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.GuardAreaAllocation.User.Name)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="GuardAreaAllocation.Id" />
        <input type="submit" value="حذف" class="btn btn-danger" /> 
        <a class="btn btn-outline-dark" asp-page="./Index">انصراف</a>
    </form>
</div>

<div>
    @section BottomPanel {
        <a class="btn btn-danger rounded-circle" asp-page="./Index"><i class="fa fa-arrow-left"></i></a>
    }
</div>