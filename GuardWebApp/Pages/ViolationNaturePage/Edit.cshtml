@page
@model GuardWebApp.Pages.ViolationNaturePage.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h4>ویرایش ماهیت تخلف</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ViolationNature.Id" />
            <div class="form-group">
                <label class="control-label">نام ماهیت تخلف</label>
                <input asp-for="ViolationNature.Name" class="form-control" />
                <span asp-validation-for="ViolationNature.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-primary m-2" />
            </div>
        </form>
    </div>
</div>

<div>
    @section BottomPanel {
        <a class="btn btn-danger rounded-circle" asp-page="Index"><i class="fa fa-arrow-left"></i></a>
    }
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
